<h1>New physician</h1>
<%= simple_form_for(@physician, url: physicians_path) do |f| %>
  <%= f.error_notification %>
	
	<%= render 'form', f: f %>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

<%= link_to 'Back', physicians_path %>
